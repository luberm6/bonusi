# Workspace Developer Rules for Antigravity (AGY)

## Android Build Artifact Rule

Whenever creating or rebuilding the Android App Bundle (`app-release.aab`) or release APKs, ALWAYS copy/save the generated release file directly to the root of the workspace (`/Users/iluxa/bonusi/app-release.aab`) so it is immediately accessible for Google Play Store upload.
